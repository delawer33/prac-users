# Postgres container bootstrap
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST_PORT=

# Users API host port mapping
USERS_HOST_PORT=

# Users service database DSN
postgres_url=

# Kafka consumer
kafka_bootstrap_servers=
kafka_order_feedback_created_topic=
kafka_order_feedback_created_dlq_topic=
kafka_consumer_group_id=users-order-feedback-created

# Order feedback events consumer retry
consumer_retry_max_attempts=
consumer_retry_initial_seconds=
consumer_retry_cap_seconds=

# Local app runtime
app_host=
app_port=
app_reload=
